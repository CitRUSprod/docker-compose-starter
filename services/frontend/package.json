{
    "name": "frontend",
    "private": true,
    "scripts": {
        "dev": "nodemon -w ./src -e js,json -x \"node ./esbuild/dev && pnpm start\"",
        "build": "node ./esbuild/prod",
        "start": "node ./dist/main"
    },
    "devDependencies": {
        "esbuild": "0.8.56",
        "nodemon": "2.0.7",
        "source-map-support": "0.5.19"
    },
    "dependencies": {
        "fastify": "3.13.0"
    }
}
