version: "3.3"

services:
    nginx:
        container_name: nginx
        image: ${NGINX_IMAGE}
        restart: always
        network_mode: host
        environment:
            FRONTEND_HOST: localhost
        volumes:
            - ../nginx:/etc/nginx/templates
